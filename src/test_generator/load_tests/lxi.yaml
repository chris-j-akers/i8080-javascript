---
test_suite:
  enable: True
  generator_function: load_tests.generate_lxi
  description: 'LXI Register'
  output_file_name: '/load/lxi.reg.test.js'
  header: |
    const Computer = require('../../computer');
    const i8080 = require('../../i8080');
  footer: |
    });
  boilerplate: |
    const max_value_to_test = {max_value_to_test};
    const c = new Computer();

    for (let word = 0x0000; word <= max_value_to_test; word++) {{
        c.cpu.{opcode_function}(word);
        expect(c.cpu.registers.{low_byte_register}).toEqual(word & 0x00FF);
        expect(c.cpu.registers.{high_byte_register}).toEqual((word >> 8) & 0x00FF);
    }}
    }});

  tests:

    - test:
      name: LXI B,d16
      comment: 
      opcode_function: lxi_b
      low_byte_register: B
      high_byte_register: C 
      
    - test:
      name: LXI D,d16
      comment: 
      opcode_function: lxi_d
      low_byte_register: D
      high_byte_register: E    
    
    - test:
      name: LXI H,d16
      comment: 
      opcode_function: lxi_h
      low_byte_register: H
      high_byte_register: L